// import { Suspense } from "react";
// import api from "@/shared/api";

const NewReact = () => {
  // const posts = api.get('/');
  return (
    <div>
      NewReact!
    </div>
    // <Suspense fallback={<p>Loading...</p>}>
      // {/* <div className="bg-slate-200">NewReact!</div> */}
    // {/* </Suspense> */}
  );
};

export default NewReact;
